<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Proposal Management</title>
    <style>
        body {
            margin: 0;
            background: linear-gradient(to right, #F2F2F2, #E6E6E6);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            height: 100vh;
            overflow: hidden; /* Hide overflow to prevent scroll bars */
            display: flex;
            justify-content: center;
            align-items: center;
            background-image: url('path_to_your_background_image'); /* Add your background image here */
            background-size: cover;
            background-repeat: no-repeat; /* Prevent background pattern repetition */
            position: relative;
        }

        .overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.3); /* Overlay color with opacity */
        }

        .container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            z-index: 1; /* Ensure content is above the background */
        }

        .cube {
            width: 200px;
            height: 200px;
            margin: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            cursor: pointer;
            border-radius: 10px; /* Rounded corners */
            overflow: hidden; /* Hide overflow to prevent child elements from overflowing */
            position: relative; /* Relative positioning for absolute child elements */
            transition: transform 0.3s ease; /* Smooth transition on hover */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2); /* Add a subtle shadow */
        }

        .cube:hover {
            transform: scale(1.1); /* Scale up on hover */
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.3); /* Increase shadow on hover */
        }

        .view-proposal {
            background-color: #007BFF; /* Blue */
        }

        .create-proposal {
            background-color: #28A745; /* Green */
        }

        .cube a {
            color: #fff;
            text-decoration: none;
            font-size: 20px;
            position: absolute; /* Absolute positioning to center text */
            z-index: 1; /* Ensure text is above other elements */
        }

        .cube::after {
            content: ''; /* Create a pseudo-element for additional graphics */
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 150px;
            height: 150px;
            background-image: url('path_to_your_image'); /* Add your image here */
            background-size: cover;
            opacity: 0.7; /* Adjust opacity as needed */
            z-index: 0; /* Ensure the background image is behind other elements */
            transition: opacity 0.3s ease; /* Smooth transition on hover */
            border-radius: 50%; /* Make the pseudo-element circular */
        }

        .cube:hover::after {
            opacity: 0.9; /* Increase opacity on hover */
        }
    </style>
</head>
<body>
    <div class="overlay"></div>
    <div class="container">
        <div class="cube view-proposal">
            <a href="/viewinnovation">View Proposal</a>
        </div>
        <div class="cube create-proposal">
            <a href="/innovation">Create Proposal</a>
        </div>
    </div>
</body>
</html>
